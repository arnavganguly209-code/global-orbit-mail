/**
 * GLOBAL ORBIT MAIL — Auth Module Index
 * Wire NextAuth / Auth.js here in a later phase.
 */

export {
  ROLE_PERMISSIONS,
  hasPermission,
  hasAnyPermission,
  authArchitecture,
  type AuthSessionShape,
} from "@/lib/auth/permissions";
